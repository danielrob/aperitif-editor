import styled from 'styled-components'
import { addClassNames } from 'utils'

const EditorWrapper = styled.div.attrs(addClassNames(
))`
  background-color: #fffccf;
  padding: 50px 30px;
  color: #1e1e1e;
`

export default EditorWrapper
